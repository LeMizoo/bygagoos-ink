# backend/.env.docker - Configuration Docker
# ===========================================
# DOCKER BACKEND CONFIG
# ===========================================
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://frontend:5173

# ===========================================
# DATABASE - PostgreSQL dans Docker
# Use environment variables in CI / compose to provide credentials
# ===========================================
DATABASE_URL=postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/bygagoos_ink

# ===========================================
# SECURITY
# Replace with real secrets via environment variables in production
# ===========================================
JWT_SECRET=${JWT_SECRET:-replace_with_secure_secret}
JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET:-replace_with_secure_refresh_secret}

# ===========================================
# CORS
# ===========================================
CORS_ORIGIN=http://frontend:5173
CORS_CREDENTIALS=true

# ===========================================
# APP CONFIG
# ===========================================
APP_NAME=ByGagoos-Ink-Docker
APP_VERSION=1.0.0
DEBUG=true

# ===========================================
# PUBLIC URL (dans le réseau Docker)
# ===========================================
PUBLIC_URL=http://backend:3001
API_URL=http://backend:3001/api